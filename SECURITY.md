# 安全与隐私

本项目是本地插件，不提供远程服务。运行时会在你的电脑上启动一个只监听 `127.0.0.1` 的本地服务，用来连接插件界面、下载素材并导入 DaVinci Resolve。

## 本地运行

- 服务只绑定 `127.0.0.1`，外部网络无法直接访问。
- 插件页面访问 API 时需要随机令牌。
- 令牌只在当前会话中使用，进程退出后失效。
- 页面不会把令牌放进第三方请求的 Referer。

## API Key

- 你的素材平台 API Key 保存在本机 `~/.davinci_plugins/api_keys.json`。
- 仓库和发布包只包含空模板 `api_keys.example.json`。
- 请不要把自己的 `api_keys.json` 发给别人，也不要提交到 GitHub。

## 下载与缓存

- 下载的素材默认保存在 `~/.davinci_plugins/cache`。
- 缓存文件会写入 sidecar metadata，便于追踪来源和授权。
- 插件会限制下载地址类型，避免读取本地文件或访问本机保留地址。

## 授权提示

插件会尽量记录素材来源、作者、原始链接和授权链接，并在导入前提示风险。

默认策略：

- 非商用素材默认阻止导入。
- 需要署名的素材会提示确认。
- 授权信息会尽量写入 Resolve metadata。

素材最终授权仍以素材平台条款为准。商用前请自行确认。

## 网络说明

插件会访问你配置的第三方素材平台，例如 Pexels、Pixabay、Freesound、Mixkit，以及翻译服务。除这些搜索、预览、下载请求外，本项目没有自己的远程服务器。
